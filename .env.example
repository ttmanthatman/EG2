# Set to "true" to use mock LLM responses instead of real DeepSeek API.
USE_MOCK_LLM=false

# Dev server port
PORT=3000

# Disable TLS verification (for proxy environments)
# NODE_TLS_REJECT_UNAUTHORIZED=0
